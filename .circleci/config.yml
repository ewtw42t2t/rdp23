jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - aws-cli/install
workflows:
  example-workflow:
    jobs:
      - example-job